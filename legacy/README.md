# Legacy freeze (Phase 0)

These files are the last known working MILCOM demo sources, copied unchanged
from `MILCOM_demo` for reference and emergency runs.

Prefer the installable package:

```bash
pyoranris run -c configs/offline_sim.yaml
```

See `docs/GOLDEN_PATH.md`.
